<div class="columns-container">
  <div class="left-col {{if this.isFullWidth "left-col-expand" "left-col-collapse"}}">
    <h1>{{#link-to "index"}}Card Themer{{/link-to}}</h1>
    <i>{{@cardName}}</i>
    <div class="themer-controls">
      <Checkbox @setChecked={{fn this.setChecked "showEditingPane"}} @checked={{this.showEditingPane}} @label="Show editing pane" />
      <Checkbox @setChecked={{fn this.setChecked "isFullWidth"}} @checked={{this.isFullWidth}} @label="Responsive resize" />
    </div>
    <div class="display-wrapper">
      {{yield}}
    </div>
    <style  {{did-insert this.initialRender}} id="card-styles"></style>
  </div>

  <div class="right-col transparent {{if this.showEditingPane "slide-in-right" "slide-out-right"}}" >
    <div class="code-panel-heading">
      CSS Editor
      <Hint @beaconName="csshint">
        Edit the code below to add some styles! For example, try changing the 
        <code>
          font-size
        </code>
        to a different number, and see an instant preview.
      </Hint>
    </div>
    {{code-editor
      language="css"
      code=this.css
      updateCode=(action "updateCode")
      theme="vs-dark"
      onReady=(action "editorReady")
      readOnly=false
    }}
    <div class="code-panel-heading">
      HTML Viewer
      <Hint @beaconName="htmlhint">
          Use this to look up the classes and HTML elements you could write CSS styles for.
          If you need to make changes to this, visit the Card Builder page instead.
      </Hint>
    </div>
    {{code-editor
      language="html"
      code=this.markup
      theme="vs-dark"
    }}
  </div>
</div>
